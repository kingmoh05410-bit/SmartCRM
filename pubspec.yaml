name: smartcrm
description: A Smart CRM app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  call_log: ^0.2.3

flutter:
  uses-material-design: true
